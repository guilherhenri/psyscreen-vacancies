name: CI/CD Pipeline

on:
  push:
    branches: [main]

jobs:
  tests:
    uses: ./.github/workflows/test.yml
    secrets: inherit

  deploy:
    needs: [tests]
    uses: ./.github/workflows/deploy.yml
    secrets: inherit
